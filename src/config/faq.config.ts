/**
 * FAQ lives in lib/faq.ts today (Wifi Code copy).
 * Re-export so shell code can import from config; migrate content here later if desired.
 */
export { faqSections, type FaqItem, type FaqSection } from "@/lib/faq"
